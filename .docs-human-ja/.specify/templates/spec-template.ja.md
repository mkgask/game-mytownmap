# フィーチャー仕様: [FEATURE NAME]

**フィーチャーブランチ**: `[###-feature-name]`  
**作成日**: [DATE]  
**ステータス**: Draft  
**入力**: ユーザー記述: "$ARGUMENTS"  
**ドキュメント更新**: 開発ドキュメントを変更する前に、必ずリポジトリルートの `CONTRIBUTING.md` を確認し、その指示に従ってください。PRに参照を明記してください。

**アプリ配置 (固定)**: `src/apps/mytownmap`  
**パッケージマネージャ (必須)**: bun  
**必須スタック**: PixiJS (2D レンダリング), bitECS (ECS ランタイム), bun (ビルド/パッケージ), Cloudflare Pages (ホスティング), New Relic (エラーのみ), Playwright (UI/E2E テスト)  
**アーキテクチャ焦点**: ECS ファースト、決定的/シード付きシミュレーション；車両は道路のみ走行し重ならない；渋滞緩和が目的；日次スケジュール（住宅→工場/商店、工場/商店→ランダム商店、商店→住宅）で就業/収容上限を尊重；プレイヤー介入は道路/建物の追加・変更・削除に限定；UI/E2E テストは Playwright を使用。

## 憲章整合チェック *(ストーリー作成前に必須)*

- アプリは `src/apps/mytownmap` に置き、Cloudflare Pages にデプロイ可能なSPAである。
- bun を唯一のパッケージマネージャとし、npm/pnpm/yarnは未承認で禁止。
- ECS は bitECS を用い、小さく単一責任でシード決定的なシステムとする。
- レンダリングは PixiJS。車両は道路のみ・重なりなし。渋滞が主課題。日次移動スケジュールと収容上限を遵守。
- プレイヤー介入は道路/建物の追加・変更・削除に限定。その他はガバナンス承認が必要。
- 可観測性: New Relic（エラーのみ）、Cloudflare Web Analytics は任意/プライバシー配慮、設定は外部化。
- UI/E2E テストは Playwright 必須。他フレームワークはガバナンス承認が必要。
- ゲームコード構造マトリクスを厳守: 依存は一方向 `app/usecase → feature → infrastructure`、feature間の直接呼び出しは禁止（usecase経由）。UIはECS stateを参照のみ（変更は usecase→ECS/system）。永続化は `infrastructure/persistence` のDTO経由のみ。Routingは `feature/routing` 実装＋選択は `usecase`。シードPRNGは `feature/rng`。設定/定数/feature flagsは `feature/config`。アセットは `infrastructure/assets`、Pixi/UIは `infrastructure/ui`/`ui/pixi`/`ui/screens`/`ui/hud`。

## ユーザシナリオとテスト *(必須)*

### ユーザストーリー1 - [短いタイトル] (優先度: P1)

[このユーザジャーニーを平易に記述]

**なぜこの優先度か**: [価値と優先理由]

**独立テスト**: [このストーリー単体で検証する方法]

**受入れシナリオ**:

1. **前提** [初期状態], **操作** [アクション], **結果** [期待]
2. **前提** [初期状態], **操作** [アクション], **結果** [期待]

---

### ユーザストーリー2 - [短いタイトル] (優先度: P2)

[記述]

**なぜこの優先度か**: [理由]

**独立テスト**: [単体検証方法]

**受入れシナリオ**:

1. **前提** [初期状態], **操作** [アクション], **結果** [期待]

---

### ユーザストーリー3 - [短いタイトル] (優先度: P3)

[記述]

**なぜこの優先度か**: [理由]

**独立テスト**: [単体検証方法]

**受入れシナリオ**:

1. **前提** [初期状態], **操作** [アクション], **結果** [期待]

---

[必要に応じて追加]

### エッジケース

- [境界条件] のときどうなるか？
- [エラーシナリオ] をどう扱うか？

## 要件 *(必須)*

### 機能要件

- **FR-001**: システムは [具体的な能力]
- **FR-002**: システムは [検証/バリデーション]  
- **FR-003**: ユーザーは [主要な操作] できる
- **FR-004**: システムは [データ要件] を満たす
- **FR-005**: システムは [挙動] を行う
- **FR-006**: [NEEDS CLARIFICATION 例示]
- **FR-007**: [NEEDS CLARIFICATION 例示]

### 主要エンティティ *(データが絡む場合)*

- **[Entity 1]**: [意味と主要属性]
- **[Entity 2]**: [意味と関係]

## 成功指標 *(必須)*

### 測定可能な成果

- **SC-001**: [例: 主要タスクを2分以内に完了]
- **SC-002**: [例: 1000同時ユーザーで性能劣化なし]
- **SC-003**: [例: 初回成功率90%]
- **SC-004**: [例: サポート問い合わせ50%削減]
